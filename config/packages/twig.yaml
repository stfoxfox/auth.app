twig:
    paths: ['%kernel.project_dir%/templates']

    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        # the value is the service's id
        locales: '%app_locales%'
